# 📝 博客更新操作说明

## 一、发布新文章

### 第 1 步：创建文章文件

在 `_posts` 文件夹中新建一个 `.md` 文件，文件名格式：

```
YYYY-MM-DD-简短英文标题.md
```

例如：`2026-06-08-my-second-post.md`

### 第 2 步：写入文章内容

复制 `_posts/_模板.md` 作为起点，填写你的内容。

### 第 3 步：推送

打开终端，逐行执行以下三条命令：

```powershell
# 进入博客目录
cd "D:\工作\人工智能\2026人工智能\AI赋能\AI写作"

# 提交所有更改
& "C:\Program Files\Git\bin\git.exe" add -A
& "C:\Program Files\Git\bin\git.exe" commit -m "发布新文章"

# 推送到 GitHub
& "C:\Program Files\Git\bin\git.exe" push
```

推送后等待 1-2 分钟，访问 https://YiYi97.github.io 即可看到新文章。

---

## 二、Markdown 常用语法

| 效果 | 写法 |
|------|------|
| **加粗** | `**文字**` |
| *斜体* | `*文字*` |
| 标题 | `## 二级标题` `### 三级标题` |
| 链接 | `[显示文字](网址)` |
| 图片 | `![描述](图片地址)` |
| 列表 | `- 项目` 或 `1. 项目` |
| 引用 | `> 引用内容` |
| 代码 | `` `行内代码` `` |
| 代码块 | ` ``` ` 开头和结尾 |
| 分割线 | `---` |

---

## 三、修改已有文章

1. 直接在 `_posts` 中找到对应的 `.md` 文件
2. 修改内容
3. 重新执行第 3 步的推送命令

---

## 四、修改网站设置

编辑根目录下的 `_config.yml` 文件，可以修改：
- `title` — 网站标题
- `description` — 网站描述
- `author` — 作者名
- `email` — 联系邮箱

---

## 五、自定义样式

编辑 `assets/css/style.css`，常用的颜色变量在文件开头的 `:root` 中：

```css
:root {
    --accent: #6c5ce7;   /* 主题色 */
    --bg: #ffffff;        /* 背景色 */
    --text: #2d3436;      /* 文字色 */
    ...
}
```

修改后推送即可生效。
